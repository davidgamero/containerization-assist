{{- if .Values.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ca.fullname" . }}-web
  labels:
    {{- include "ca.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ca.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
{{- end }}
