apiVersion: v1
kind: Service
metadata:
  name: {{ include "ca.fullname" . }}-api
  labels:
    {{- include "ca.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ca.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
